import React from 'react';
import Appbar from './app-bar';
import AppFrame from './AppFrame';

function Page() {
  return (
    <AppFrame>
      <Appbar />;
    </AppFrame>
  );
}

export default Page;
